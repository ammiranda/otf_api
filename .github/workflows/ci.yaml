name: CI

on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ["stable"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Tidy (library)
        run: go mod tidy && git diff --exit-code -- go.mod go.sum

      - name: Build (library)
        run: go build ./...

      - name: Test (library)
        run: go test -v -count=1 ./...

      - name: Tidy (CLI)
        run: cd cmd/otf-cli && go mod tidy && git diff --exit-code -- go.mod go.sum

      - name: Build (CLI)
        run: cd cmd/otf-cli && go build ./...
